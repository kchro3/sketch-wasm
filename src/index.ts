import * as wasm from "../pkg/sketch_wasm";

export const BloomFilter = wasm.BloomFilter;
export const HyperLogLog = wasm.HyperLogLog;
export const CountMinSketch = wasm.CountMinSketch;
export const init = wasm.init;
